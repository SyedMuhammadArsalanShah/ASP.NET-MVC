@model _11_Auth_MVC.Models.Signup

@{
	ViewBag.Title = "Index";
}


<h2>Index</h2>

@if (Session["UserEmail"] != null)
{
	<h1>welcome @Session["UserEmail"].ToString() </h1>

}else
{
	<h1>@Html.ActionLink("Login", "Login", User)</h1>
}